#include <stdio.h>

int main() {
    int a[5] = {1, 2, 3, 4, 5};
    int b[3] = {6, 7, 8};
    int c[8], i, j;

    for (i = 0; i < 5; i++) c[i] = a[i];
    for (j = 0; j < 3; j++) c[i + j] = b[j];

    printf("Merged Array: ");
    for (i = 0; i < 8; i++) printf("%d ", c[i]);

    return 0;
}
